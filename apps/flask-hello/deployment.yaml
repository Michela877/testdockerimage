apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-hello
  namespace: flask-hello
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-hello
  template:
    metadata:
      labels:
        app: flask-hello
    spec:
      containers:
      - name: flask
        image: test.local/michela877/flask-hello:1.0v
        ports:
        - containerPort: 8081
        env:
        - name: FLASK_ENV
          value: "production"
